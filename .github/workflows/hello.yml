name: Hello Workflow

on: workflow_dispatch

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello to users
        run: echo "Hello dosto"

      - name: Say hello to user
        run: echo "Hello $USER"
  details:
    runs-on: ubuntu-latest
    steps:
      - name: show what are the versions of python,docker etc
        run: docker --version && python --version

    


        

    
   


